version: 2
jobs:
    build:
        docker:
            - image: circleci/python:2.7-stretch-browsers
        steps:
            - run: |
                mv project circlecidemo
                cd circlecidemo
                pwd
                git status
                git log
                git branch newbranch
                git status
                git log
                
